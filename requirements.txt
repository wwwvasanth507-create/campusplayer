# Campus Video Player - Requirements

# Core Web Framework
Flask==3.1.3
Werkzeug==3.1.8

# Flask Extensions
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-SocketIO==5.6.1
Flask-Caching==2.4.0
Flask-Limiter==4.1.1

# Optional Flask Extensions
flasgger==0.9.7.1
Flask-Mail==0.10.0
Flask-Assets==2.1.0

# PDF Generation
reportlab==4.5.1

# AI Assistant (optional)
google-generativeai==0.8.3
aiofiles
# HTTP requests
requests==2.34.2

# Optional real-time worker support
eventlet==0.41.0

# Security & Auth
email-validator==2.3.0
cryptography>=42.0.5

# Task Queue & Cache
celery==5.6.3
redis==8.0.0

# Browser automation (optional)
selenium==4.14.0

# System & Deployment
python-dotenv==1.2.2
psutil==7.2.2
gunicorn==23.0.0

# Notes
# - FFmpeg is required as a system binary and is invoked via subprocess.
# - `Flask-Mail`, `Flask-Assets`, `flasgger`, `selenium`, and `eventlet` are optional.
# - `redis` is required for Celery broker/backend configuration.
# - `gunicorn` is an optional production server, not imported in application code.
# - `python-engineio` and `python-socketio` are installed transitively by Flask-SocketIO.
# - `python-dotenv` is used by `app.py` and `celery_config.py` to load environment variables.
openpyxl==3.1.5
